// 1.14 전처리기와의 첫 만남.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <algorithm>
using namespace std;

//#define MY_NUMBER "Hello, World"
//#define MAX(a, b) (((a)>(b))? (a): (b))

#define LIKE_APPLE

void doSomething();

int main()
{
	//cout << MY_NUMBER << endl;
	//cout << MAX(1 + 3, 2) << endl;
	//cout << std::max(1+ 5, 5) << endl;
	doSomething();

	return 0;
}

